git clone `https://github.com/fernandogallegoh75/BootCampProfesG706.git`
# Comandos
1 **Comandos Git**
📑 `git --version`
`cd` me sirve para navegar entre Carpetas
`cd ..` para devolverme una Carpeta
`git init` para iniciar Repositorio vacío
`git status` para verificar el estado de Archivos
`Set-ExecutionPolicy Unrestricted` para habilitar ejecución de comandos
`Set-ExecutionPolicy -ExecutionPolicy Unrestricted -Scope CurrentUser` para habilitar ejecución de comandos si el anterior falla
`git add .` para agregar los archivos a mi git (caja)
`gitt commit -m "first commit"` para sellar la caja
`git clone` más la ruta del git a clonar, para clonar Repositorio
`dir` ver directorios
`cls` limpia la Terminal


`python --version`
crear un entorno virtual en python
`python -m venv env312`
activarlo `env312\scripts\activate`

`deactivate` para desactivar el entorno

`pip list` mostrar la lista de librerías instaladas en nuestro entorno
`##` librerias - para subtitulos de python
`pip install pandas numpy matplotlib`
pandas - para manejo de datos `pip install pandas`
numpy - calculos matematicos `pip install numpy`
matplotlib - graficos `pip install matplotlib`
`python.exe -m pip install --upgrade pip` para actualizar el pip o libreria
`git remote` me va a mostrar a dónde está enlazado mi Repositorio
<!--  --> Esto aparece dando simultaneamente las teclas Alt Shift A
`<!-- usuario: admin password: admin123-->` para colocar una Contraseña cambiable de Administrador a la página
`<hr>` para agregar una línea horizontal

<p> Esta etiqueta es tipo párrafo 📋<p>
<
Con las teclas Control y Espacio nos muestra opciones al escribir un commit
<br> es un salto de línea
Con las Teclas Alt Shift y f mi código se acomoda
Atajo para duplicar: se señala los que se va a duplicar y se presionan las teclas Alt Shift y flecha hacia arriba o abajo, según donde se quiera duplicar.

Como agregamos a Python a las variables de entorno, para trabajar directamente en el ejecutor de Python, escribimos solo `python`
Para salir del ejecutor directo de Python, simplemente damos click a las teclas `Control z`
Para escargar la librería `nltk.download('punkt')`
`import nltk` Implementación de la librería
sklearn.feature extraction.text = Es una librería especializada para IA